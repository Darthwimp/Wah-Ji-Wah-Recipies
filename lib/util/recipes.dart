import 'dart:ui';

import 'package:flutter/material.dart';

class Butter_Chicken extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Color(0xFFDD5353),
        title: Text("Butter Chicken"),
        centerTitle: true,
      ),
      body: Center(
        child: Text(""),
      ),
    );
  }
}
